import Modal from "@/components/modal/modal";
import { ModalProps } from "@/components/modal/util";
import React from "react";

const ModalTest = (props: ModalProps) => {
  return <Modal {...props}>ModalTest</Modal>;
};

export default ModalTest;
